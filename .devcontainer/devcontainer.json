{
    "name": "Sturdy-Carnival Dev",
    "build": {
        "dockerfile": "Dockerfile",
        "context": ".."
    },
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-vscode.cpptools",
                "ms-python.python",
                "ms-python.vscode-pylance",
                "ms-azuretools.vscode-docker"
            ]
        }
    },
    "mounts": [
        "source=${localWorkspaceFolder},target=/workspace,type=bind"
    ],
    "workspaceFolder": "/workspace",
    "postCreateCommand": "python3 -m venv --upgrade-deps .venv && .venv/bin/python -m pip install --upgrade pip && for f in Python/*/requirements.txt; do .venv/bin/python -m pip install -r \"$f\"; done"
}